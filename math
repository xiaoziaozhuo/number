#include<stdio.h>
#include<math.h>
int main()
{
  float x;
  scanf("%f",&x);
  if(x>6)
  printf("%.3f",(5.0/27.0)*(x*x+4*x-6));
  if(x>0 && x<=6)
  printf("%.3f",log(16)/log(3)+x);
  if(x<=0)
  printf("%.3f",(23.0/7.0)*fabs(x*x*x+4));
  return 0;
  
  
  
}
